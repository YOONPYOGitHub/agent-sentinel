# Known issues

Last reviewed **2026-09-14**.

| ID     | Issue                                                      | Impact                                                                                                                                                                                                                                                                                                                      | Unblock condition                                                                                                                                                                                                                             |
| ------ | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| KI-002 | Active Front Door has no evidenced API mutation rule       | The active Front Door WAF has no evidenced custom mutation rule. `BlockApiMutationPreAuth` is on the stopped Application Gateway and does not protect Front Door; read-only safety depends on `writeEnabled=false`, so write activation is prohibited                                                                       | Keep writes false, activate and validate JWT read-only, then separately review, deploy, and rediscover an exact Front Door mutation rule before any write-stage readiness                                                                     |
| KI-005 | Only one Foundry source supplies live inventory            | Multi-source aggregation is implemented, but the deployed portfolio does not yet validate a second tenant/project or another platform                                                                                                                                                                                       | Authorize a second Foundry project through same-tenant RBAC or cross-tenant federation, then activate Entra and OTel on the shared source model                                                                                               |
| KI-006 | Application Gateway is HTTP-only and management-controlled | It is unsuitable as the final sign-in redirect and may stop                                                                                                                                                                                                                                                                 | Use the active Front Door HTTPS edge; add final domain ownership when available                                                                                                                                                               |
| KI-007 | Private CI runner may be deallocated                       | Queued builds do not start until the VM runs                                                                                                                                                                                                                                                                                | Start the runner before builds or add approved scheduling                                                                                                                                                                                     |
| KI-008 | Runner identity has image-push permission only             | Automated infrastructure deployment and what-if fail                                                                                                                                                                                                                                                                        | Grant separately reviewed deployment RBAC or retain manual deployment                                                                                                                                                                         |
| KI-009 | Service Tree record has no valid IcM node                  | Microsoft Entra rejects API and SPA app registration attribution with `Service not found`                                                                                                                                                                                                                                   | Provide two SFI trusted service-admin identities, complete required on-call contact profiles, onboard to IcM, and allow up to one day for propagation                                                                                         |
| KI-010 | Manifest ingestion activation is blocked                   | The authenticated API and jobs composition are implemented, but live writes-false and the public mutation posture prevent API ingestion                                                                                                                                                                                     | Validate Administrator/write scopes and the exact public-edge mutation path before enabling ingestion; unauthenticated ingestion will not be added                                                                                            |
| KI-012 | Legacy manifest runtime claims may be uncorrelatable       | `runtime_observed` claims without the optional exact source binding remain explicitly `not-correlatable`                                                                                                                                                                                                                    | Producers add all four source-binding fields; verification then uses only non-synthetic Azure Monitor evidence                                                                                                                                |
| KI-013 | Live OTel workspace has no analysis-ready rows             | The current 30-day audit has 0 AppRequests/AppDependencies/AppTraces, and AppMetrics lacks the required agent attributes; live windows therefore remain `unknown`/`insufficient-data`                                                                                                                                       | After deployment approval, emit approved representative non-customer traffic with exact IDs, unsampled raw evidence, measured latency/tokens/cost, and explicit non-synthetic provenance; never replace missing rows with metrics or fixtures |
| KI-014 | Current Foundry agents lack Entra identity metadata        | Entra inventory is live with 335 identity nodes, but both historical and replacement validation agents expose null instance identity and null blueprint reference. Across 6 authoritative agents, no exact object/application/client or preview-confirmed Agent Identity ID is available, so 0 `RUNS_AS` edges are emitted. | Configure an agent identity or expose one authoritative source-local identity ID in Foundry metadata; never infer correlation by name, alias, owner, or fuzzy text.                                                                           |
| KI-015 | Replacement authentication is not activated                | Replacement API/SPA registrations do not yet exist, no role assignments or consent are evidenced, `AUTH_MODE=disabled`, and reviewed image digests have not been applied                                                                                                                                                    | Approve and apply the exact registration plan, complete least-privilege consent/assignments, deploy reviewed digests with writes false, and run active-edge plus token-driven validation for every role                                       |
| KI-016 | Power Platform inventory lacks app-only authorization      | Microsoft documents delegated inventory access and explicitly excludes preview Power Platform RBAC roles; the runtime cannot safely activate ResourceQuery                                                                                                                                                                  | Wait for a production-supported unattended permission with enforceable scope; do not use query filters or legacy management-app registration as RBAC                                                                                          |
| KI-018 | Defender for Cloud Apps source is currently empty          | The source is ready, but the new tenant returns zero alerts and activities in the bounded 24-hour window; no Defender evidence nodes are present                                                                                                                                                                            | Retain valid-empty semantics until the tenant produces real records; never synthesize evidence or infer agent joins                                                                                                                           |
| KI-019 | Purview catalog cannot prove label usage                   | The primary label-definition source is live, but catalog metadata does not show content, usage, users, activity, agent attribution, trust, or compliance                                                                                                                                                                    | Keep evidence scope explicit; add a separate supported usage source only after independent permission and privacy review                                                                                                                      |
| KI-020 | Teams organization catalog is empty                        | The replacement source is ready and returns zero organization entries; this proves only an empty bounded catalog result                                                                                                                                                                                                     | Retain explicit empty-state semantics; do not infer installation, distribution, trust, or agent coverage                                                                                                                                      |
| KI-021 | Repository changes are newer than the deployed API         | The live web/API/jobs SHAs and digests are known, but Entra activation hardening and deterministic release-review v2 are later repository changes; a repository pass does not attest the deployed API.                                                                                                                      | Build and deploy an approved full SHA surgically, record fresh revisions/digests and sanitized configuration evidence, then rerun release review.                                                                                             |

Missing evidence is an expected state, not a healthy state. Do not close an
issue by replacing a failed live signal with mock data.
