# Known issues

Last reviewed **2026-08-30**.

| ID     | Issue                                                      | Impact                                                                                                                                                           | Unblock condition                                                                                                                                     |
| ------ | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| KI-002 | WAF blocks all pre-auth API mutations                      | Authenticated Terra POST and remediation execution cannot use the public edge                                                                                    | Validate JWT write scopes, then narrow the rule                                                                                                       |
| KI-005 | Only one Foundry source supplies live inventory            | Multi-source aggregation is implemented, but the deployed portfolio does not yet validate a second tenant/project or another platform                            | Authorize a second Foundry project through same-tenant RBAC or cross-tenant federation, then activate Entra and OTel on the shared source model       |
| KI-006 | Application Gateway is HTTP-only and management-controlled | It is unsuitable as the final sign-in redirect and may stop                                                                                                      | Use the active Front Door HTTPS edge; add final domain ownership when available                                                                       |
| KI-007 | Private CI runner may be deallocated                       | Queued builds do not start until the VM runs                                                                                                                     | Start the runner before builds or add approved scheduling                                                                                             |
| KI-008 | Runner identity has image-push permission only             | Automated infrastructure deployment and what-if fail                                                                                                             | Grant separately reviewed deployment RBAC or retain manual deployment                                                                                 |
| KI-009 | Service Tree record has no valid IcM node                  | Microsoft Entra rejects API and SPA app registration attribution with `Service not found`                                                                        | Provide two SFI trusted service-admin identities, complete required on-call contact profiles, onboard to IcM, and allow up to one day for propagation |
| KI-010 | Manifest ingestion activation is blocked                   | The authenticated API and jobs composition are implemented, but live writes-false and the public mutation posture prevent API ingestion                          | Validate Administrator/write scopes and the exact public-edge mutation path before enabling ingestion; unauthenticated ingestion will not be added    |
| KI-011 | Manifest claims are non-authoritative and capped           | Exact authoritative objects can now be correlated, but free-form claim values are not compared or endorsed and adapter evidence never overrides first-party data | Add typed comparison only where a supported first-party contract exposes an equivalent field; never compare free-form text heuristically              |
| KI-012 | Legacy manifest runtime claims may be uncorrelatable       | `runtime_observed` claims without the optional exact source binding remain explicitly `not-correlatable`                                                         | Producers add all four source-binding fields; verification then uses only non-synthetic Azure Monitor evidence                                        |
| KI-013 | Live OTel windows have sparse samples                      | The connector is ready and queries measured AppRequests, but current agent windows remain typed `insufficient-data` below the ten-sample floor                   | Emit representative non-customer validation traffic over baseline and observed windows; never replace missing samples with synthetic success          |
| KI-014 | Current Foundry agents lack Entra identity metadata        | Entra inventory is live, but no exact object/application/client ID is available on the six agents, so no `RUNS_AS` edge is emitted                               | Configure an agent identity or expose an authoritative identity ID in Foundry metadata; do not infer correlation by display name                      |
| KI-015 | Only Viewer has completed live role validation             | Analyst, Approver, Administrator, and write-scope behavior are proven in tests but not yet with live assigned principals                                         | Assign least-privilege test principals and run the token-driven capability validator for every role                                                   |
| KI-016 | Power Platform ResourceQuery still returns `403`           | Dedicated AI Reader and Power Platform Reader assignments exist, but service-principal inventory access has not propagated or is not accepted                    | Re-test after propagation and escalate the exact read-only service-principal authorization path if `403` persists                                     |
| KI-017 | Agent 365 package inventory lacks tenant licensing         | `CopilotPackages.Read.All` is assigned, but the tenant lacks M365 E5 prerequisite and Agent 365 licensing                                                        | Complete the managed-environment trial request, then validate the read-only package list                                                              |
| KI-018 | Defender for Cloud Apps evidence is authorization-gated    | The official OAuth v1 alert/activity list connector is implemented but disabled; no live MDCA evidence is present and no agent attribution is possible           | Approve licensing/API availability, exact tenant portal URLs, secretless credentials, and tenant-admin `Investigation.Read`; never infer agent joins  |
| KI-019 | Purview catalog cannot prove label usage                   | The primary label-definition source is live, but catalog metadata does not show content, usage, users, activity, agent attribution, trust, or compliance         | Keep evidence scope explicit; add a separate supported usage source only after independent permission and privacy review                              |
| KI-020 | Teams backend is disabled for the tenant                   | `AppCatalog.Read.All` is assigned, but Graph returns `AADSTS500014`; no catalog evidence can be read                                                             | Provision the approved Teams Enterprise trial, then validate catalog-only evidence without inferring installation or agent coverage                   |

Missing evidence is an expected state, not a healthy state. Do not close an
issue by replacing a failed live signal with mock data.
