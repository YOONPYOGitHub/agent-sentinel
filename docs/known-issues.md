# Known issues

Last reviewed **2026-08-28**.

| ID     | Issue                                                      | Impact                                                                                                                                           | Unblock condition                                                                                                                                                     |
| ------ | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| KI-002 | WAF blocks all pre-auth API mutations                      | Authenticated Terra POST and remediation execution cannot use the public edge                                                                    | Validate JWT write scopes, then narrow the rule                                                                                                                       |
| KI-003 | OTel sources are not activated                             | Multi-source routing is implemented, but the UAMI has no workspace query role and live agents do not emit the required validated attributes      | Approve read-only workspace RBAC, instrument agent requests, configure matching `AZURE_MONITOR_SOURCES_JSON`, and validate bounded windows                            |
| KI-005 | Only one Foundry source supplies live inventory            | Multi-source aggregation is implemented, but the deployed portfolio does not yet validate a second tenant/project or another platform            | Authorize a second Foundry project through same-tenant RBAC or cross-tenant federation, then activate Entra and OTel on the shared source model                       |
| KI-006 | Application Gateway is HTTP-only and management-controlled | It is unsuitable as the final sign-in redirect and may stop                                                                                      | Use the active Front Door HTTPS edge; add final domain ownership when available                                                                                       |
| KI-007 | Private CI runner may be deallocated                       | Queued builds do not start until the VM runs                                                                                                     | Start the runner before builds or add approved scheduling                                                                                                             |
| KI-008 | Runner identity has image-push permission only             | Automated infrastructure deployment and what-if fail                                                                                             | Grant separately reviewed deployment RBAC or retain manual deployment                                                                                                 |
| KI-009 | Service Tree record has no valid IcM node                  | Microsoft Entra rejects API and SPA app registration attribution with `Service not found`                                                        | Provide two SFI trusted service-admin identities, complete required on-call contact profiles, onboard to IcM, and allow up to one day for propagation                 |
| KI-010 | Manifest ingestion activation is blocked                   | The authenticated API and jobs composition are implemented, but live writes-false and the public mutation posture prevent API ingestion          | Validate Administrator/write scopes and the exact public-edge mutation path before enabling ingestion; unauthenticated ingestion will not be added                    |
| KI-011 | Manifest claims are non-authoritative and capped           | Adapter evidence stays at confidence ≤ 0.7 and never overrides first-party connector data                                                        | Correlate manifest claims against a first-party connector or a runtime telemetry connector                                                                            |
| KI-012 | Manifest runtime evidence is declaration-only              | `runtime_observed` claims are trusted declarations, not independently verified telemetry                                                         | Configure `azure-monitor-otel` and cross-check manifest runtime claims against collected spans                                                                        |
| KI-013 | Behavior drift shows synthetic observations in mock mode   | Drift data in the Observability page and AgentDetail page is labeled `[SYNTHETIC]`; the deployment has no configured live telemetry              | Configure `azure-monitor-otel`; live mode will query real `ObservationWindow` objects without a mock fallback                                                         |
| KI-014 | Entra sources lack tenant-admin consent                    | Multi-source Graph inventory, exact Foundry source correlation, health, and API/jobs wiring are implemented, but live inventory remains disabled | Consent `Application.Read.All` in each target tenant, configure matching `ENTRA_SOURCES_JSON`, and validate bounded v1.0 inventory; review preview consent separately |
| KI-015 | Only Viewer has completed live role validation             | Analyst, Approver, Administrator, and write-scope behavior are proven in tests but not yet with live assigned principals                         | Assign least-privilege test principals and run the token-driven capability validator for every role                                                                   |

Missing evidence is an expected state, not a healthy state. Do not close an
issue by replacing a failed live signal with mock data.
